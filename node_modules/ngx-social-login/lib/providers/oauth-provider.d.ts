import { Observable } from 'rxjs';
import { Provider } from '../models/provider';
import { SocialUser } from '../models/social-user';
export declare abstract class OauthProvider {
    private readonly ID_PREFIX;
    constructor(provider: Provider, src: string, onload: () => void, async?: boolean);
    abstract login(): Observable<SocialUser>;
    abstract logout(): Observable<any>;
}
